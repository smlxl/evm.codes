## Dynamic gas

If the value is changed from zero to non-zero, then the cost is {gasPrices|sstoreSet}. Otherwise the cost is {gasPrices|sstoreReset}.

## Gas refunds

If the value is changed from non-zero to zero, adds {gasPrices|sstoreRefund} to the gas refunds. See [gas refunds](/about).
