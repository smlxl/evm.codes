---
fork: Constantinople
group: Comparison & Bitwise Logic Operations
---

## Notes

Shift the bits towards the least significant one. The bits moved before the first one are discarded, the new bits are set to 0.

## Stack input

0. `Integer`: number of bits to shift to the right.
1. `Integer`: value.

## Stack output

0. `Integer`: the shifted value. If the shift value is bigger than 255, returns 0.

## Examples

| Input | Output | * | Input | Output |
|------:|-------:|-|------:|-------:|
| `1` | `1` | * | `4` | `0xF` |
| `2` | | * | `0xFF` | |

## Error cases

The transaction is reverted in those cases:
- Not enough gas.
- Not enough values on the stack.
