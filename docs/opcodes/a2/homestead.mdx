## Dynamic gas

    dynamic_gas = {gasPrices|logTopic} * topic_count + {gasPrices|logData} * byte_count + memory_expansion_cost

For LOG2, `topic_count` is 2.
The memory expansion cost explanation can be found [here](/about).
